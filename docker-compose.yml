services:
  property-checklist:
    build: .
    ports:
      - "5002:5000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
